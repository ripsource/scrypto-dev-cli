import { ProgrammaticScryptoSborValue } from '@radixdlt/babylon-gateway-api-sdk';
import { SborSchema } from '../sborSchema';
import { ParsedType } from './struct';
export declare class ArraySchema<T extends SborSchema<unknown>> extends SborSchema<ParsedType<T>[]> {
    private itemSchema;
    constructor(itemSchema: T);
    validate(value: ProgrammaticScryptoSborValue, path: string[]): boolean;
    parse(value: ProgrammaticScryptoSborValue, path: string[]): ParsedType<T>[];
}
