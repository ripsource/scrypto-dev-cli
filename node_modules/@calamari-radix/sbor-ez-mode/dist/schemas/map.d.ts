import { ProgrammaticScryptoSborValue } from '@radixdlt/babylon-gateway-api-sdk';
import { SborSchema } from '../sborSchema';
export interface MapDefinition<T, U> {
    key: SborSchema<T>;
    value: SborSchema<U>;
}
export declare class MapSchema<K, V> extends SborSchema<Map<K, V>> {
    private definition;
    constructor(definition: MapDefinition<K, V>);
    validate(value: ProgrammaticScryptoSborValue, path: string[]): boolean;
    parse(value: ProgrammaticScryptoSborValue, path: string[]): Map<K, V>;
}
